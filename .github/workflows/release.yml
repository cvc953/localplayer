name: Android Release Build

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v2.1.3, etc.
  workflow_dispatch:
    inputs:
      version:
        description: "Versión a publicar (ej. 1.2.3)"
        required: true
        type: string

jobs:
  build:
    name: Build Android Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Prepare signing keystore
        env:
          SIGNING_KEYSTORE_BASE64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
          ANDROID_SIGNING_STORE_PASSWORD: ${{ secrets.ANDROID_SIGNING_STORE_PASSWORD }}
          ANDROID_SIGNING_KEY_ALIAS: ${{ secrets.ANDROID_SIGNING_KEY_ALIAS }}
          ANDROID_SIGNING_KEY_PASSWORD: ${{ secrets.ANDROID_SIGNING_KEY_PASSWORD }}
        run: |
          if [[ -n "$SIGNING_KEYSTORE_BASE64" ]]; then
            echo "$SIGNING_KEYSTORE_BASE64" | base64 -d > app/release.keystore
            echo "ANDROID_SIGNING_STORE_FILE=app/release.keystore" >> $GITHUB_ENV
          fi
          if [[ -n "$ANDROID_SIGNING_STORE_PASSWORD" ]]; then echo "ANDROID_SIGNING_STORE_PASSWORD=$ANDROID_SIGNING_STORE_PASSWORD" >> $GITHUB_ENV; fi
          if [[ -n "$ANDROID_SIGNING_KEY_ALIAS" ]]; then echo "ANDROID_SIGNING_KEY_ALIAS=$ANDROID_SIGNING_KEY_ALIAS" >> $GITHUB_ENV; fi
          if [[ -n "$ANDROID_SIGNING_KEY_PASSWORD" ]]; then echo "ANDROID_SIGNING_KEY_PASSWORD=$ANDROID_SIGNING_KEY_PASSWORD" >> $GITHUB_ENV; fi
      
      - name: Extract version from tag
        id: extract_version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]] && [[ -n "${{ github.event.inputs.version }}" ]]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="dev-$(date +%Y%m%d%H%M%S)"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"
      
      - name: Build Release APK
        run: |
          if [[ -n "$ANDROID_SIGNING_STORE_FILE" && -n "$ANDROID_SIGNING_STORE_PASSWORD" && -n "$ANDROID_SIGNING_KEY_ALIAS" && -n "$ANDROID_SIGNING_KEY_PASSWORD" ]]; then
            ./gradlew assembleRelease
          else
            echo "Signing secrets missing; building Debug APK as fallback" >&2
            ./gradlew assembleDebug
          fi
        
      - name: Collect APK and checksums
        run: |
          mkdir -p release
          # Prefer signed release APK; fallback to debug if release not built
          APK_PATH=$(find app/build/outputs/apk/release -type f -name "*.apk" ! -name "*unsigned*" | head -n 1)
          if [[ -z "$APK_PATH" ]]; then
            APK_PATH=$(find app/build/outputs/apk/debug -type f -name "*.apk" | head -n 1)
          fi
          if [[ -z "$APK_PATH" ]]; then
            echo "No APK produced under app/build/outputs/apk/release" >&2
            exit 1
          fi
          cp "$APK_PATH" "release/LocalPlayer-${{ steps.extract_version.outputs.VERSION }}.apk"
          (cd release && sha256sum "LocalPlayer-${{ steps.extract_version.outputs.VERSION }}.apk" > checksums.txt)
       
      
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: localplayer-release-${{ steps.extract_version.outputs.VERSION }}
          path: |
            release/LocalPlayer-${{ steps.extract_version.outputs.VERSION }}.apk
            release/checksums.txt
          retention-days: 30
      
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.extract_version.outputs.VERSION }}
          target_commitish: ${{ github.sha }}
          files: |
            release/LocalPlayer-${{ steps.extract_version.outputs.VERSION }}.apk
            release/checksums.txt
          body: |
            ## Local Player v${{ steps.extract_version.outputs.VERSION }}
            
            ### 📥 Descarga
            
            Descarga el APK y sigue las instrucciones de instalación en el README.
            
            ### ✅ Checksums
            
            ```
            $(cat release/checksums.txt)
            ```
            
            ### 📋 Requisitos
            
            - Android 7.0 (API 24) o superior
            - Aproximadamente 10 MB de espacio libre
            
            ### 🔧 Instalación
            
            1. Descarga el APK
            2. Habilita "Instalar desde fuentes desconocidas" en tu dispositivo
            3. Abre el APK e instala
            4. Concede los permisos necesarios al abrir la app
            
            ### 📝 Notas
            
            - Primera instalación: la app escaneará automáticamente tu biblioteca
            - Las actualizaciones posteriores mantendrán tu configuración
            
            ---
            
            **Instalación completa**: [Ver README](https://github.com/cvc953/localplayer/blob/master/README.md)
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
